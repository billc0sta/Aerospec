is_even := (n) {
    is_odd := (n) { 
       -> n == 0 ? Bool.false : is_even(n - 1)
    }
    -> n == 0 ? Bool.true : is_odd(n - 1)
}

IO.print(is_even(0), "\n")
IO.print(is_even(1), "\n")
IO.print(is_even(2), "\n")
IO.print(is_even(3), "\n")
IO.print(is_even(4), "\n")
IO.print(is_even(5), "\n")