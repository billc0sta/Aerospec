mult_table := [0<i<=12;;[0<j<=12;;i*j]]

>> 0 <= i < len(mult_table) {
    row = mult_table[i]
    // Print each row
    >> 0 <= j < len(row) {
        print(row[j], "\t")
    }
    print("\n")  // Print a newline for better readability
}